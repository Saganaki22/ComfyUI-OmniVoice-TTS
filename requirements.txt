# Manual dependency reference — DO NOT uncomment, these are managed by install.py
# to prevent PyTorch from being downgraded by omnivoice's pinned torch==2.8.* requirement
#
# omnivoice        --no-deps  (pinned to torch==2.8.*, installed with --no-deps to protect ComfyUI's PyTorch)
# pydub                       (imported by omnivoice at module level)
# soundfile        --no-deps
# scipy            --no-deps
# librosa          --no-deps
# sentencepiece    --no-deps
# jieba            --no-deps
